'use strict';

describe('myApp.attendance module', function() {

  beforeEach(module('myApp.attendance'));

  describe('attendance controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var attendanceCtrl = $controller('AttendanceCtrl');
      expect(attendanceCtrl).toBeDefined();
    }));

  });
});